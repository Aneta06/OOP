package pizzaoopoverloadedconstructor;

public class OOPInfo {

    // overloaded constructors = multiple constructors within a class with the same name but have different parameters
    //                            name + parametres = signature
    //                         = více konstruktorů v rámci třídy se stejným názvem, ale s různými parametry
}
